{
    "properties": {
        "filesystem_paths": {
            "type": "array",
            "items":{
                "type": "string",
                "format": ["file", "directory"]
            },
            "minItems": 1
        }
    },
    "required": ["filesystem_paths"]
}
