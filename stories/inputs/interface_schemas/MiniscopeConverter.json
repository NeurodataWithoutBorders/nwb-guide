{
    "required": [],
    "properties": {
        "MiniscopeConverter": {
            "properties": {
                "folder_path": {
                    "format": "path",
                    "type": "string",
                    "description": "The path to the main Miniscope folder."
                },
                "user_configuration_file_path": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Path to the Miniscope acquisition User Config JSON file (named 'UserConfigFile.json' in the Miniscope documentation and source code). When provided, the converter uses this configuration to interpret the folder hierarchy and device names instead of relying on a fixed directory structure.",
                    "default": null
                },
                "verbose": {
                    "default": false,
                    "type": "boolean"
                }
            },
            "required": [
                "folder_path"
            ],
            "type": "object",
            "additionalProperties": false
        }
    },
    "type": "object",
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "source.schema.json",
    "title": "Source data schema",
    "description": "Schema for the source data, files and directories",
    "version": "0.1.0"
}
